spring.application.name=LinguaAI

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/lingua_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=9129811515
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Web Configuration
server.port=8080

# SQL Initialization
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# CORS Support
spring.mvc.cors.allowed-origins=http://localhost:5173

# Email Configuration (Mock/Fake for local dev to prevent crashes)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# Gemini Configuration
gemini.api.key=AIzaSyBU0y4YrTzWYFyPG5rMwkhOtSWPr4f51JM
ai.enabled=true